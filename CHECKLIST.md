# ✅ Чек-лист запуску SkyBooking

## 📋 Перед запуском

### 1. Перевірка встановлених компонентів

- [ ] **PHP 7.4+** встановлено
  ```bash
  php --version
  ```

- [ ] **PostgreSQL 12+** встановлено
  ```bash
  psql --version
  ```

- [ ] **PDO PostgreSQL extension** активовано
  ```bash
  php -m | grep pdo_pgsql
  ```

---

## 🗄️ Налаштування бази даних

### 2. Створення бази даних

- [ ] Створено базу даних `aviation_booking`
  ```bash
  createdb -U postgres aviation_booking
  ```

- [ ] Виконано `schema.sql`
  ```bash
  psql -U postgres -d aviation_booking -f src\schema.sql
  ```

- [ ] Виконано `test_data.sql` (опціонально)
  ```bash
  psql -U postgres -d aviation_booking -f src\test_data.sql
  ```

- [ ] Перевірено створення таблиць
  ```bash
  psql -U postgres -d aviation_booking -c "\dt"
  ```

**Очікуваний результат:** 8 таблиць (customers, passengers, airlines, airports, flights, bookings, tickets, payments)

---

## ⚙️ Конфігурація проекту

### 3. Налаштування підключення

- [ ] Відредаговано `includes/config.php`
  - [ ] `DB_HOST` (зазвичай `localhost`)
  - [ ] `DB_NAME` (`aviation_booking`)
  - [ ] `DB_USER` (ваш користувач PostgreSQL)
  - [ ] `DB_PASS` (ваш пароль)

**Приклад:**
```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'aviation_booking');
define('DB_USER', 'postgres');
define('DB_PASS', 'your_password');
```

---

## 🚀 Запуск проекту

### 4. Варіант A: PHP Built-in Server (рекомендовано для розробки)

- [ ] Запустити `start.bat` (Windows)
  ```bash
  start.bat
  ```

**АБО**

- [ ] Запустити вручну:
  ```bash
  cd public
  php -S localhost:8000
  ```

- [ ] Відкрити браузер: http://localhost:8000

---

### 5. Варіант B: Apache/Nginx (для production)

#### Apache:

- [ ] Скопіювати проект до директорії сервера (наприклад, `C:\xampp\htdocs\skybooking`)

- [ ] Створити Virtual Host:
  ```apache
  <VirtualHost *:80>
      ServerName skybooking.local
      DocumentRoot "C:/xampp/htdocs/skybooking/public"
      
      <Directory "C:/xampp/htdocs/skybooking/public">
          AllowOverride All
          Require all granted
      </Directory>
  </VirtualHost>
  ```

- [ ] Додати в hosts файл (`C:\Windows\System32\drivers\etc\hosts`):
  ```
  127.0.0.1 skybooking.local
  ```

- [ ] Перезапустити Apache

- [ ] Відкрити: http://skybooking.local

---

## 🧪 Перевірка роботи

### 6. Тестування основних функцій

- [ ] **Головна сторінка** завантажується без помилок
  - Відкрити: http://localhost:8000/

- [ ] **Пошук рейсів** відображає форму
  - Перейти: Пошук рейсів

- [ ] **База даних** підключається коректно
  - Немає помилок "could not connect to server"

- [ ] **Список рейсів** показує дані з БД
  - Обрати аеропорти та дату
  - Натиснути "Знайти рейси"

- [ ] **Реєстрація** працює
  - Спробувати зареєструвати тестовий акаунт

- [ ] **Авторизація** працює
  - Увійти з тестовим акаунтом:
    - Email: test@skybooking.com
    - Пароль: password123

---

## 🔍 Діагностика проблем

### Якщо сайт не відкривається:

- [ ] Перевірити, чи запущено PHP сервер
- [ ] Перевірити порт 8000 (може бути зайнятий)
- [ ] Спробувати інший порт:
  ```bash
  php -S localhost:8080
  ```

### Якщо помилка підключення до БД:

- [ ] PostgreSQL запущено?
  ```bash
  # Windows
  net start postgresql-x64-12
  
  # або через Services
  ```

- [ ] Правильні дані в `config.php`?
- [ ] База даних створена?
  ```bash
  psql -U postgres -l
  ```

### Якщо не показуються рейси:

- [ ] Виконано `test_data.sql`?
- [ ] Перевірити дані в БД:
  ```sql
  psql -U postgres -d aviation_booking
  SELECT COUNT(*) FROM flights;
  ```

### Якщо помилки PHP:

- [ ] Перевірити версію PHP (>= 7.4)
- [ ] Включити відображення помилок (в config.php):
  ```php
  ini_set('display_errors', 1);
  error_reporting(E_ALL);
  ```

---

## 📱 Перевірка функціоналу

### 7. Повний флоу тестування

- [ ] Пошук рейсів
  - [ ] Обрати Київ (KBP) → Париж (CDG)
  - [ ] Дата: завтра
  - [ ] Пасажирів: 2

- [ ] Вибір рейсу
  - [ ] Побачити список рейсів
  - [ ] Натиснути "Обрати" на будь-якому

- [ ] Авторизація
  - [ ] Увійти або зареєструватись

- [ ] Вибір місць
  - [ ] Побачити схему літака
  - [ ] Обрати 2 місця
  - [ ] Натиснути "Продовжити"

- [ ] Дані пасажирів
  - [ ] Заповнити для 2 пасажирів
  - [ ] Натиснути "Продовжити"

- [ ] Підтвердження
  - [ ] Перевірити дані
  - [ ] Поставити галочку
  - [ ] Підтвердити

- [ ] Оплата
  - [ ] Заповнити дані картки
  - [ ] Оплатити

- [ ] Квиток
  - [ ] Побачити квиток з QR-кодом
  - [ ] Роздрукувати (опціонально)

---

## 🎉 Готово!

Якщо всі пункти виконані - проект готовий до використання!

### Наступні кроки:

1. **Ознайомтесь з документацією:**
   - [ ] README.md - загальний опис
   - [ ] USER_GUIDE.md - інструкція користувача
   - [ ] DEVELOPER_GUIDE.md - для розробників

2. **Додайте тестові дані:**
   - [ ] Створіть кілька тестових рейсів
   - [ ] Додайте різні аеропорти та авіакомпанії

3. **Налаштуйте під себе:**
   - [ ] Змініть кольори в `css/style.css`
   - [ ] Додайте логотип
   - [ ] Налаштуйте email сповіщення

---

## 📞 Потрібна допомога?

### Перевірте документацію:
- `README.md` - загальна інформація
- `DATABASE_SETUP.md` - налаштування БД
- `USER_GUIDE.md` - як користуватись
- `DEVELOPER_GUIDE.md` - технічні деталі

### Перевірте логи:
```bash
# PHP логи
tail -f /path/to/php/error.log

# PostgreSQL логи
tail -f /var/log/postgresql/postgresql-12-main.log
```

---

**Успіхів! 🚀**
